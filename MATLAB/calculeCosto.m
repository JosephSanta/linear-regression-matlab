function J = calculeCosto(X, y, theta)
%CALCULECOSTO calcula el costo de regresión lineal
%   J = CALCULECOSTO(X, y, theta) calcula el costo de utilizar theta como
%   el parametro para ajustar la regresión lineal a los datos X e y

% Inicializar algunos valores útiles
m = length(y); % numero de ejemplos de entrenamiento

% Debe retornar la siguiente variable correctamente
J = 0;

% ====================== SU CÓDIGO AQUÍ ======================
% Instrucciones: Calcule el costo de una selección particular de
%                theta, el valor lo debe guardar en J.





% =========================================================================

end
